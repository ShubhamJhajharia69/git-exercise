# git-exercise
## Git Exercise - Assignment Summary

### Completed Tasks:
- Local repository initialization with staging/unstaging operations
- Remote operations with push, pull, and partial staging
- Branching (`g1`, `b1`, `b2`, `b3`) and Pull Request workflow
- Reset operations (`--soft`, `--hard`, `--mixed`)
- Interactive staging with `git add -p`
- Complex staging + stash + conflict resolution scenario
- Merge conflicts resolution
- `rm` and `revert` operations
- Fork and Pull Request demonstration

### Repository Status:
All branches merged, cleaned up, and pushed to remote.

**Last Commit:** Complex stash conflict resolved

